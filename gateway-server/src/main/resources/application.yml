server:
  port: 8080
spring:
  profiles:
    active: scenario1
  application:
    name: gateway-server
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    loadbalancer:
      nacos:
        enabled: true

logging:
  level:
    org.springframework.cloud.gateway: DEBUG